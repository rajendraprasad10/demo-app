# role.yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: dev
  name: developer-role

rules:
- apiGroups: ["", "apps"]
  resources:
  - pods
  - deployments
  - services

  verbs:
  - get
  - list
  - watch
  - create
  - update
